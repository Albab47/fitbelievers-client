import React from 'react';

const PageTitle = ({title}) => {
    return (
        <h1 className='text-4xl mt-10 mb-5 text-center font-display text-dark'>{title}</h1>
    );
};

export default PageTitle;